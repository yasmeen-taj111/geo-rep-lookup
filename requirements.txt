# Root requirements.txt — used by Railway at deploy time.
# Mirrors backend/requirements.txt and adds aiofiles (needed for
# FastAPI's StaticFiles mount to serve the frontend).

fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
aiofiles==23.2.1